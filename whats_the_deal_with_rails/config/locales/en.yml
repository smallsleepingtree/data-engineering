# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  unauthorized: Until the admin authorizes your account, you're stuck here.
  actions: Actions

  titles:
    application: WHAT'S THE DEAL
    limbo: YOU ARE IN LIMBO
    sub:
      upload_order_log: 'Upload Order Log'
      order_log_index: 'Order Logs'
      orders_index: 'Orders'
      sign_in: 'Sign In'
      sign_in_or_register: 'Sign In or Register'
      user_registration: User Registration
      users_index: User Administration
    nav:
      order_logs: Order Logs
      users: Users

  helpers:
    submit:
      order_log:
        create: "Upload File"
      session:
        create: "Sign In"

  errors:
    messages:
      not_tab_separated: "does not appear to be a tab-delimited file"
      bad_openid_url: "does not appear to be a valid OpenID URL"
      or_openid_url: "required unless OpenID URL provided"
      or_email: "required unless email provided"
      has_invalid_orders: "contains bad order data and can't be parsed"
    attributes:
      password:
        unnecessary: "not required if using OpenID"

  session:
    sign_in: 'Sign in'
    sign_out: 'Sign out'
    welcome:
      html: 'Welcome, <strong>%{addressee}</strong>.'
    needed: You must be signed in to do anything at all.
    created: Welcome!  You have successfully signed in.
    redundant: But you're already signed in!  What's the big idea?
    destroyed: You are now signed out.
    failed_authentication: You entered an invalid email address or password.
    form:
      legend: 'Sign In'

  user:
    created: Your user account has been created.
    authorized: The user has been authorized and can now move freely about the website.
    rejected: That schmuck has been rejected. Probably not the first time.
    form:
      legend: Register for an Account
    administration:
      authorize: Authorize
      reject: Reject

  order_log:
    new: 'Upload New Order Log'
    form:
      legend: 'Order Log Upload'
      missing_filename: '(no filename)'
    uploaded: 'Your order log is now in the database!  Go pet a kitten to celebrate.'

  activerecord:
    attributes:
      order_log:
        id: 'ID'
        filename: 'Filename'
        created_at: 'Created at'
        gross_revenue: 'Gross Revenue'

  mail:
    authorization:
      pending:
        subject: Some peon is begging you for authorization
      authorize:
        subject: Your request has been granted, you lucky fool!
      reject:
        subject: "Rejection is like the moon: spherical and reflective."

  openid:
    failure: "Using OpenID is hard. %{message}"
    cancelled: "You cancelled your OpenID authentication!  Why?! %{message}"